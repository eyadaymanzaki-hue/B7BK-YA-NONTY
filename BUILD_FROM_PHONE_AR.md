# عمل APK من الموبايل فقط

1. افتح GitHub من Chrome وسجل الدخول.
2. اعمل New repository باسم `b7bk-ya-nonty-apk`.
3. ارفع كل ملفات المشروع إلى المستودع، بما فيها مجلد `.github/workflows`.
4. افتح تبويب Actions.
5. اختار `Build B7BK YA NONTY APK` ثم Run workflow.
6. بعد نجاح البناء افتح صفحة التشغيل الأخيرة، وانزل إلى Artifacts.
7. نزّل `B7BK-YA-NONTY-APK`، فك الضغط، ثم افتح `app-debug.apk` وثبته.

ملاحظة: هذا APK تجريبي (debug) مناسب للتثبيت المباشر على الهاتفين. التطبيق يحتاج الإنترنت لكي يتصل بـ Supabase ويجلب البيانات والوسائط المشتركة.
